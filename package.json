{
  "name": "algoritmo-genetico",
  "version": "1.0.0",
  "description": "Algoritmo genético para encontrar valores que minimizem funções de segundo grau",
  "main": "/app/index.js",
  "scripts": {
    "postinstall": "bower install",
    "prestart": "npm install",
    "start": "http-server -a 0.0.0.0 -p 9000 -o",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Kevilin Rodrigues",
  "license": "ISC",
  "dependencies": {
    "bower": "^1.6.8",
    "http-server": "^0.8.5"
  }
}
